import numpy as np

# ==========================================================================================================
# Murtada G. Hameed | المرجع من كتابتي
# ==========================================================================================================

# 1- مصفوفة أحادية البعد (1D Array)
arr1 = np.array([1, 2, 3, 4])

# 2- مصفوفة ثنائية البعد (2D Array)
arr2 = np.array([[1, 2], [3, 4]])

# عدد الأقواس المربعة يشير إلى عدد الصفوف بينما عدد عناصر كل قوس يشير إلى عدد الأعمدة (الترقيم يبدأ من الصفر)
# في المثال أدناه: الرقم 1 يشير إلى الصف الثاني، والرقم 0 يشير إلى العمود الأول
# print(arr2[1, 0])

# بعض الوظائف المهمة
# print(arr1.ndim)   # عدد الأبعاد (Dimensions)
# print(arr2.size)   # معرفة إجمالي عدد عناصر المصفوفة
# print(arr2.dtype)  # معرفة نوع بيانات عناصر المصفوفة (مثل int64 أو float64)
# print(arr1.shape)  # معرفة شكل المصفوفة (عدد الصفوف والأعمدة)

# ==========================================================================================================

arr3 = np.zeros((3, 2))        # إنشاء مصفوفة مليئة بالأصفار وتحديد (الصفوف، الأعمدة)
arr4 = np.ones((3, 2))         # إنشاء مصفوفة مليئة بالرقم 1
arr5 = np.full((3, 2), 5)      # إنشاء مصفوفة وتعبئتها بقيمة مخصصة (هنا الرقم 5)
arr6 = np.eye(3)               # مصفوفة الوحدة (Identity Matrix)
arr7 = np.arange(0, 10, 2)     # توليد أرقام من 0 إلى ما قبل 10 بفارق (خطوة) 2
reshaping = arr2.reshape(2, 2) # إعادة تشكيل المصفوفة لأبعاد جديدة
flatting = arr2.flatten()      # تحويل المصفوفة إلى أحادية البعد (تسطيح)

# ================================== RANDOM ================================================

arr8 = np.random.rand(3, 2)   # أرقام عشوائية موجبة بين 0 و 1 -> float
# print(np.random.randn(3, 2))# أرقام عشوائية تتبع التوزيع الطبيعي (غالباً بين -3 و 3) -> float
# print(np.random.randint(1, 20, (3, 3))) # أرقام عشوائية صحيحة (integers) بين 1 و 19 داخل مصفوفة 3x3

# ================================== OPERATIONS ============================================
arr9 = np.array([[1, 2, 3], [5, 6, 7]])
arr10 = np.array([[8, 9, 10]])
arr11 = np.array([[1, 2, 3], [4, 5, 6]])

# العمليات الرياضية (تتم عنصر مقابل عنصر بفضل الـ Broadcasting)
# print(arr9 + arr10)
# print(arr9 * arr10)
# print(arr9 ** 2)
# print(np.cbrt(arr9)) # الجذر التكعيبي
# print(arr11[:, 0] + arr11[:, 1])

# دالات إحصائية وحسابية:
# print(np.mean(arr10)) # إيجاد المتوسط الحسابي
# print(np.sum(arr10))  # مجموع العناصر
# print(np.max(arr10))  # أكبر قيمة
# print(np.min(arr10))  # أصغر قيمة

# ========================== SORTING and REPEATING ============================

# print(np.sort([1, 2, 4, 3]))            # ترتيب الأعداد تصاعدياً
# print(np.unique([1, 2, 2, 2, 3, 4, 5])) # إزالة العناصر المتكررة واستخراج القيم الفريدة

# ======================== الدمج (Joining) =============================
# الدمج عبر المحور 0 (إضافة كصفوف جديدة)
connect = np.concatenate((arr9, arr10), axis=0)

# الدمج العمودي والأفقي:
# print(np.vstack((arr9, arr10)))  # الدمج العمودي (فوق بعض)
# تم تعديل الدمج هنا ليكون بين (arr9 و arr11) ليصبح ددمجاً أفقياً صحيحاً لتوافق الأبعاد
hstack = np.hstack((arr9, arr11)) # الدمج الأفقي (بجانب بعض)

# ========================================= view && copy =======================

arr12 = arr11.view() # إنشاء رؤية (أي تعديل يؤثر على المصفوفة الأصلية)
arr12[0, 0] = 100

arr13 = np.array([1, 2, 3])
arr14 = arr13.copy() # إنشاء نسخة مستقلة تماماً في الذاكرة
arr14[0] = 22

# =============================================== searching =============================

searr = np.array([1, 2, 3, 4, 5, 4, 4, 4])
index = np.where(searr == 4) # إيجاد مواقع (indexes) العناصر التي تساوي 4
# print(index)

# =========================================================================================
array = np.array([[1, 2, 3, 4], [5, 6, 7, 8]])

arr = np.arange(0, 10, 2)        # نحدد (البداية، النهاية، الخطوة)
lins = np.linspace(10, 1000, 10) # توليد 10 أرقام متساوية التباعد تماماً بين 10 و 1000

# =================================== flatten and transpose =========================================================
flatting = array.flatten() # تسطيح المصفوفة
trans = array.T            # مقلوب المصفوفة (جعل الصفوف أعمدة والأعمدة صفوف)

# indexing and slicing (الفهرسة والتقطيع)
# print(array[1, :]) # طباعة كل عناصر الصف الثاني
# print(array[:, 1]) # طباعة كل عناصر العمود الثاني


# =================================== Vectorization & One-Hot Encoding ===============================================
# من أهم أفكار هذه المكتبة في الـ ML: عندما يكون لدينا عدة مدن على سبيل المثال ووضعنا للمدينة الأولى رقم 1 وللمدينة الثانية رقم 3، 
# سيفهم الكمبيوتر أن الثانية أهم من الأولى رياضياً، لذلك نحول كل كلمة إلى عمود من صفر وواحد (One-Hot Encoding) باستخدام مصفوفة الوحدة.

vect = np.array([1, 0, 3, 0])

vect_eye = np.eye(4)[vect]
print(vect_eye)

# murtada G. hameed © 2026